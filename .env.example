# Production (e.g. Vercel): your Render web service URL, no trailing slash.
# Leave unset for local dev — Vite proxies /api to the backend (see vite.config.js).
# VITE_API_BASE_URL=https://your-service.onrender.com
